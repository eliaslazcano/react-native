import { StyleSheet } from 'react-native';

const style = StyleSheet.create({
  cabecalho: {
    flexDirection: 'row',
    alignItems: 'center',
  },
  avatar: {
    width: 40,
    height: 40,
    margin: 10,
    borderRadius: 30,
  },
});

export default style;
